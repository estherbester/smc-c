/*

Largest Int


*/

#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#define END_CHAR 0




int main (void)
{
    int number = -1,  // initialize so that max and number go throguh the do-while loop once
        max=0,
        max_count=0;
    char input;

    printf("Enter a series of numbers; enter 0 to finish: ");

    /*
     get the input:

     scanf string


     parse the input for integers:


     for each integer:
        if integer > max
            max = integer
        else
            if integer == max
                max_count++



    */

    return 0;
}
