#include <stdlib.h>
#include <math.h>
#include <stdio.h>
#include <string.h>


int main (void)
{

/*
    char ts[12];  // extra room for null character

    printf("Enter a string (ts): ");

    scanf("%17s", ts);
    printf("%lu", sizeof(ts));



    if (sizeof(ts) < 13 || sizeof(ts) > 17){
        printf("\nThat is not a valid length for a string\n");
    }
*//*
    printf("\nString was: %s", ts);
*/

    int a = 1;
    int c = 9;
    int b = 10;

    printf("%d", a/b);
    printf("%d", c/b);



    return 0;
}
